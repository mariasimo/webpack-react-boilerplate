import "./styles/main.scss";

const heading = document.createElement("h1");

heading.textContent = "Webpack: Boilerplate for React";

const root = document.getElementById("root");

root.append(heading);
